name: Check Spelling

on:
  push:

jobs:
  spellcheck:
    name: Check Spelling
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: rojopolis/spellcheck-github-actions@0.51.0
